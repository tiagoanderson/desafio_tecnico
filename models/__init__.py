# =============================================================================
# models/__init__.py
# -----------------------------------------------------------------------------
# CAMADA: Model (Backend)
#
# Este pacote reúne todos os "Models" do sistema: funções que executam
# comandos SQL (SELECT, INSERT, UPDATE, DELETE) diretamente no banco SQLite,
# usando a biblioteca padrão "sqlite3". Nenhum HTML e nenhuma rota Flask
# aparecem aqui: só acesso a dados.
# =============================================================================
